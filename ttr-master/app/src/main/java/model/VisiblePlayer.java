package model;

public class VisiblePlayer extends AbstractPlayer {
    public VisiblePlayer(String username, int numStartingCards){
        super.username = username;
        super.numCards = numStartingCards;
    }

}
